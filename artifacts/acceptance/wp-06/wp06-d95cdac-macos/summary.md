# WP-06 macOS acceptance summary

Status: `macos-accepted/windows-deferred`.

The actual loopback Infinity runtime served exact `BAAI/bge-m3` vectors from the
fingerprinted local model revision. A bounded three-file Akilli Kasa ADR fixture
proved semantic separation, repeat/batch stability, normalized finite vectors,
read-only source access and production composition wiring.

Fake feature hashing is retained only as an explicitly non-semantic baseline and
cannot activate a semantic index. Provider absence produces lexical-only degraded
retrieval. Dimension, NaN, partial batch, timeout, model/index mismatch, disclosure
and profile drift fail closed before durable mutation. Device/provider/model profile
identity is namespaced and restart-stable.

The independent verifier returned PASS. Windows/OpenCode live remote E2E, WP-07
hybrid RAG, large-corpus and cross-platform stress acceptance remain open.
